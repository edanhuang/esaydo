# EasyDo Daily - 2026-06-04

## 今日目标

- 初始化 Git 仓库并关联远端。
- 基于项目愿景文档进入 OpenSpec propose/apply 流程。
- 完成第一阶段 MVP 的本地优先桌面 App 骨架与核心闭环。
- 整理项目结构和开发环境说明。

## 今日进展

- 初始化 Git：
  - 本地分支为 `main`。
  - 远端为 `https://github.com/edanhuang/esaydo.git`。
  - Git 用户配置为 `edanhuang <1196786088@qq.com>`。
  - 初始项目文档已推送过一次，后续本轮实现尚未提交。

- OpenSpec：
  - 初始化 `openspec/` 和 `.codex/skills/`。
  - 创建 change：`implement-first-phase-mvp`。
  - 完成 `proposal.md`、`design.md`、`tasks.md`。
  - 新增能力规格：
    - `local-todo-board`
    - `weekly-worklog`
    - `local-persistence`
  - Apply 阶段任务已完成：`56/56`。
  - `openspec validate "implement-first-phase-mvp" --strict` 已通过。

- App MVP：
  - 初始化 Tauri + React + TypeScript + Vite 项目结构。
  - 接入 Tailwind CSS 和 lucide-react。
  - 实现主看板页面：
    - detail-only Todo 输入。
    - Enter 新增 Todo。
    - Tab 切换当前输入分组。
    - Shift + Tab 切换 Board View。
    - Arrow Up / Down 选择 Todo。
    - Command + Enter 完成 Todo。
    - 已完成 Todo 可取消完成。
    - 归档 Todo 默认隐藏。
  - 实现 Weekly 页面：
    - 按本周 completed_at 聚合已完成 Todo。
    - 按日期分组。
    - 支持复制 Markdown 周报内容。

- 本地持久化：
  - 使用 SQLite 作为唯一持久化数据源。
  - App 启动时初始化数据库。
  - 数据库位置为 Tauri app data 目录。
  - 已建表：
    - `todos`
    - `groups`
    - `tags`
    - `todo_groups`
    - `todo_tags`
    - `board_views`
    - `board_view_groups`
    - `todo_events`
  - 首次启动 seed 默认分组：
    - 工作
    - 学习
    - 健身
    - 生活
    - 个人项目
  - 首次启动 seed 默认 Board View：
    - 所有
    - 工作
    - 个人

- 事件日志：
  - 当前记录在 SQLite 的 `todo_events` 表中。
  - 已支持事件：
    - `created`
    - `completed`
    - `reopened`
    - `archived`
  - 今天排查发现缺少取消完成能力，已补 `reopen_todo` 并写入 `reopened` 事件。

- 文档：
  - 新增 `README.md`，记录环境依赖、Node 20 建议、安装、检查和启动命令。
  - 已确认 README 不包含本机绝对路径。
  - 项目初始化愿景文档已移动到 `docs/EasyDo_Project_Init_Document.md`。
  - 新增 daily 目录：`docs/daily/`。

## 验证结果

- `npm run test`：通过，`7/7`。
- `npm run check`：通过。
- `npm run build`：通过。
- `cargo check`：通过。
- `npm run tauri -- build`：通过。
- 已生成 macOS App：
  - `src-tauri/target/release/bundle/macos/EasyDo.app`
  - `src-tauri/target/release/bundle/dmg/EasyDo_0.1.0_x64.dmg`

## 当前项目结构变化

- 新增前端代码：`src/`。
- 新增 Tauri 后端代码：`src-tauri/`。
- 新增 OpenSpec 文件：`openspec/`。
- 新增 Codex OpenSpec skill 文件：`.codex/skills/`。
- 新增文档目录：`docs/`。
- 移动初始化文档到：`docs/EasyDo_Project_Init_Document.md`。
- 新增 daily 记录：`docs/daily/daily_20260604.md`。

## 剩余事项

- 做一次整体代码 review，重点检查：
  - Tauri command 错误处理。
  - SQLite 查询和事务边界。
  - UI 快捷键冲突。
  - done / reopened / archived 状态切换一致性。
- 决定是否正式引入 shadcn/ui；当前只是 Tailwind 手写组件，不是真正 shadcn。
- 补 Todo 详情 Popover：
  - 编辑 detail。
  - 编辑 extra_text。
  - 编辑 groups。
  - 编辑 tags。
- 补 Settings 页面：
  - 管理分组。
  - 管理标签。
  - 管理 Board View。
- 考虑增加事件日志查看/调试入口，方便排查 Todo 生命周期。
- 整理首次提交范围并提交推送。

## 风险和注意点

- 当前 `todo_events` 是领域事件日志，不是完整应用运行日志。
- 当前 Weekly 只基于 `completed_at` 查询；取消完成会清空 `completed_at`，因此会从 Weekly 中移除。
- 当前 UI 尚未做完整无障碍和视觉 polish。
- 当前 App 已可运行，但还没有做正式签名、公证和安装体验优化。
